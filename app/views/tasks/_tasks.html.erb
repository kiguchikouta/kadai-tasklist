<h3>タスク一覧</h3>
<ul>
    <% tasks.each do |task_nakami| %>
        <li><%= link_to task_nakami.id.to_s + '：' + task_nakami.content, task_nakami %> &gt; <%= task_nakami.status %></li>
    <% end %>
</ul>

<%= link_to '新規タスクの登録', new_task_path %>
