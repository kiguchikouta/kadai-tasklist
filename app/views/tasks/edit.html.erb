<h2>ID番号:<%= @task.id %>　タスクの編集</h2>

<h3>既存タスク</h3>
<p><%= @task.content %></p>

<h3>新規タスク</h3>
<%= form_with(model:@task, local:true) do |f| %>
    <%#= f.label :content, "タスク" %>
    <%= f.text_field :content %>
    <%= f.submit '登録' %>
<% end %>

<% link_to '一覧に戻る', tasks_path %>